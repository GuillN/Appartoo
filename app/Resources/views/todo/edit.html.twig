{% extends 'base.html.twig' %}
{% block body %}
    <a class="btn btn-default" href="{{ path('todo_list') }}" >Retour aux todos</a>
    <hr>
    <h2 class="page-header">Editer un todo</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}